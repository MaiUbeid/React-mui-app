runtime: nodejs10
env: flex

automatic_scaling:
  min_num_instances: 1

handlers:
 - url: /
 static_files: build/index.html
 upload: build/index.html

 - url: /(.*)
 static_files: build/\1
 upload: build/(.*)
